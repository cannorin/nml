let stepRun code =
  print code !;
  pause () !;
  code
in

let! one = stepRun <( 1 )> in

let! three = stepRun <( one + 2 )> in

let! four = stepRun <( one + three )> in

one + three + four |> print !;
one + three + four

